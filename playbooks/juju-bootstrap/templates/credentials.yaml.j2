credentials:
  {{ serverstack_cloud.region_name }}:
    {{ serverstack_cloud.auth.username }}:
        auth-type: userpass
        username: {{ serverstack_cloud.auth.username }}
        password: {{ serverstack_cloud.auth.password }}