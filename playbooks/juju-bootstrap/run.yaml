- hosts: all
  tasks:
    - name: 'Install Juju'
      command: sudo snap install juju --classic
    - name: Render juju cloud.yaml
      template:
        src: clouds.yaml.j2
        dest: clouds.yaml
    - name: Add cloud
      command: juju add-cloud --client {{ region }} clouds.yaml
    - name: 'render credentials.yaml'
      template:
        src: credentials.yaml.j2
        dest: credentials.yaml
    - name: Add credential
      command: juju add-credentials {{ region }} --client -f credentials.yaml
    - name: 'Bootstrap Controller'
      command: juju bootstrap {{ region }}/{{ region }}