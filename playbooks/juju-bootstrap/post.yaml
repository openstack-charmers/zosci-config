- hosts: all
  tasks:
    - name: 'tear down controller'
      command: >
        timeout 900 juju destroy-controller --destroy-storage --destroy-all-models -y {{ region }} ||\
        timeout 900 juju kill-controller -t 60s -y {{ region }}